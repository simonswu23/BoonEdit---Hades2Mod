local config = {
  version = 1;
  enabled = true;

  BoonChanges = {
    AirQuality = {
      Enabled = true;
    };

    AllTogether = {
      Enabled = true;
    };

    AnvilRing = {
      Enabled = true;
    };

    ArterialSpray = {
      Enabled = true;
    };

    BeachBall = {
      Enabled = true;
    };

    BloodSpree = {
      Enabled = true;
    };

    BraveFace = {
      Enabled = true;
    };

    BreakerRush = {
      Enabled = true;
    };

    BurningMeteor = {
      Enabled = true;
    };

    CardioGain = {
      Enabled = true;
    };

    CarnalPleasure = {
      Enabled = true;
    };

    ChainReaction = {
      Enabled = true;
    };

    CherishedHeirloom = {
      Enabled = true;
    };

    CryoPounder = {
      Enabled = true;
    };

    EcstaticObsession = {
      Enabled = true;
    };

    GlamourGain = {
      Enabled = true;
    };

    GloriousDisaster = {
      Enabled = true;
    };

    HarmForTheAfflicted = {
      Enabled = true;
    };

    HeartyAppetite = {
      Enabled = true;
    };

    HeavyMetal = {
      Enabled = true;
    };

    HostileEnvironment = {
      Enabled = true;
    };

    IonicGain = {
      Enabled = true;
    };

    KillerCurrent = {
      Enabled = true;
    };

    LocalClimate = {
      Enabled = true;
    };

    MeatGrinder = {
      -- Blade Rifts may spill Plasma when they hit a foe.
      Enabled = true;
    };

    MoltenTouch = {
      Enabled = true;
    };

    NaturalSelection = {
      Enabled = true;
    };

    OldGrudge = {
      Enabled = true;
    };

    Pandemonium = {
      Enabled = true;
    };

    StormImmunity = {
      Enabled = true;
    };

    PowerSurge = {
      Enabled = true;
    };

    PostHaste = {
      Enabled = true;
    };

    PremiumService = {
      Enabled = true;
    };

    ProfuseBleeding = {
      Enabled = true;
    };

    RippleEffect = {
      Enabled = true;
    };

    RousingReception = {
      Enabled = true;
    };

    ScaldingVapor = {
      Enabled = true;
    };

    SecondWind = {
      Enabled = true;
    };

    SeismicHammer = {
      Enabled = true;
    };

    ShockingLoss = {
      Enabled = true;
    };

    SmithyRush = {
      Enabled = true;
    };

    SmolderingForge = {
      Enabled = true;
    };

    StabbingRush = {
      Enabled = true;
    };

    SunWorshiper = {
      Enabled = true;
    };

    ThermalDynamics = {
      Enabled = true;
    };

    TranquilGain = {
      Enabled = true;
    };

    UnseenIre = {
      Enabled = true;
    };

    WinterHarvest = {
      Enabled = true;
    };
  };

  Debug = {
    LogScaldingVapor = false;

    GrantTraits = '';

    GrantRarity = 'Common';

    GrantPomLevel = 1;
  };
}

return config
