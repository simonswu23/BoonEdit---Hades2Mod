---@meta _
---@diagnostic disable: lowercase-global

-- Reloaded like `reload.lua`, but after every other mod has loaded. Boons that read another mod's
-- trait data go here rather than in `reload.lua`.

---@diagnostic disable: undefined-global

import 'boons/spontaneous_combustion.lua'
