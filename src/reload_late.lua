---@meta _
---@diagnostic disable: lowercase-global

-- Reloaded like `reload.lua`, but after every other mod has loaded. Boons reading another mod's
-- trait data go here rather than in `reload.lua`; nothing does at the moment.

---@diagnostic disable: undefined-global
